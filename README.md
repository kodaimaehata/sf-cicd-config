# Salesforce package.xml/build.xml configurator

- This extension provide screen for selecting / deselecting files in package.xml and build.xml

Run the `SF-CICD-Config.openConfigScreen` to create the webview.
